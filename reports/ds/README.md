# DS evidence

`05_locked_holdout.json` is a faithful JSON copy of the final report saved in notebook 05. It is committed because it contains aggregate metrics and hashes only—no images, paths, embeddings, or subject identifiers.

Do not edit its thresholds, metrics, holdout count, gates, or checkpoint hash manually. Regenerate it only from a deliberately re-run, versioned experiment with a new model version.
